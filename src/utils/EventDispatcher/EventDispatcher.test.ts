import {EventDispatcher} from './EventDispatcher';
import {expect} from 'chai';

describe.skip('EventDispatcher',()=>{
	describe('EventDispatcher()',()=>{
		it('should return an event dispatcher',()=>{
			
		});
	});
	describe('EventDispatcher(obj)',()=>{
		it('should augment the object with event dispatcher capabilities',()=>{
			
		});
	});
	describe('dispatcher.addEventListener()',()=>{
		it('should add an event listener',()=>{
			
		});	
	});
	describe('dispatcher.removeEventListener()',()=>{
		it('should remove the event listener',()=>{
			
		});
	});
	describe('dispatcher.dispatchEvent()',()=>{
		it('should trigger all attached events listeners',()=>{
			
		});
	});
});
