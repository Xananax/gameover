import {
	debounceSignal
,	DebouncedSignal
,	DebouncedSignalMixin
} from './DebouncedSignal'
import {expect} from 'chai';

describe.skip('DebouncedSignal',()=>{
	it('should return a DebouncedSignal',()=>{
		
	});
	it('should run only once if called repeteadly within `delay`',()=>{
		
	});
	it('should return sub signals that are also DebouncedSignal',()=>{
		
	});
});