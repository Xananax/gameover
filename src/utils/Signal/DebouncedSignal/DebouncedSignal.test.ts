import {
	debounceSignal
,	DebouncedSignal
,	DebouncedSignalMixin
} from './DebouncedSignal'
import {expect} from 'chai';

describe('DebouncedSignal',()=>{
	it.skip('should return a DebouncedSignal',()=>{
		
	});
	it.skip('should run only once if called repeteadly within `delay`',()=>{
		
	});
	it.skip('should return sub signals that are also DebouncedSignal',()=>{
		
	});
});