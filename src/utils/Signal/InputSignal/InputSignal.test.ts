import {InputSignal} from './InputSignal';
import {expect} from 'chai';

describe.skip('InputSignal',()=>{
	describe('InputSignal()',()=>{
		it('should return a signal',()=>{
			
		});
	});
	describe('inputSignal()',()=>{
		it('should trigger when called with a falsy or truthy value',()=>{
			
		});
		it('should not trigger when called with a nully value',()=>{
			
		});
		it('should return an object with the latest timestamp',()=>{
			
		});
	});
});
