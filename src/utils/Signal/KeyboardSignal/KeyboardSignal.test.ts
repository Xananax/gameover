import {KeyboardSignal} from './KeyboardSignal';
import {expect} from 'chai';

describe.skip('KeyboardSignal',()=>{
	describe('KeyboardSignal()',()=>{
		it('should return a combined signal',()=>{
			
		});
	});
	describe('keyboardSignal',()=>{
		it('should trigger when any of its dependencies trigger',()=>{
			
		});
		it('should trigger the same event for multiple keys',()=>{
			
		});
	});
});