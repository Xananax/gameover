import {
	TickerSignal
} from './TickerSignal';
import {
	expect
} from 'chai';

describe.skip('TickerSignal',()=>{
	describe('TickerSignal()',()=>{
		it('should return a TickerSignal',()=>{
			
		});
	});
	describe('tickerSignal',()=>{
		it('should have a `render` property which is a signal',()=>{
			
		});
		it('should have an `update` property which is a signal',()=>{
			
		});
		it('should trigger those signals on render and update',()=>{
			
		});
	});
});