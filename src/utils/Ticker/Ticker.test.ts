import {
	Ticker
} from './Ticker';
import {
	expect
} from 'chai';

describe.skip('Ticker',()=>{
	describe('Ticker()',()=>{
		it('should return a ticker',()=>{
			
		});
		it('should always return the same singleton ticker',()=>{
			
		});
	});
	describe('ticker.render',()=>{
		it('should be called at each render iteration',()=>{
			
		});
	});
	describe('ticker.update',()=>{
		it('should be called at each update iteration',()=>{
			
		});
	});
	describe('ticker.tick',()=>{
		it('should be called at every tick',()=>{
			
		});
	});
});