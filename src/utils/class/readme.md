# addClass

`addClass(node:HTMLElement)=>(class:string)=>void;`

adds a class

# removeClass

`removeClass(node:HTMLElement)=>(class:string)=>void;`

removes a class

# hasClass

`hasClass(node:HTMLElement)=>(class:string)=>boolean;`

returns true if the element has the specified class